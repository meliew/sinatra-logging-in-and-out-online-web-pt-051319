<!DOCTYPE html>
<html>
  <head>
    <meta charset="UTF-8">
    <title>title</title>
    <link rel="stylesheet" href="stylesheets/bootstrap.css" type="text/css">
  </head>
  <body id="page-top">

<% if Helpers.is_logged_in?(session_hash) %>
<h1>Welcome<%= Helpers.current_user(session_hash).username %></h1>
<h3>Your Balance: <%= Helpers.current_user(session_hash).balance %></h3>

<h4><a href="/logout">Log Out</a></h4>
<% else%>
  You Must <a href="/">Log In</a> to View Your Balance

<% end %>
  </body>
</html>
